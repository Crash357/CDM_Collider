"""
CDM Collider — Properties module.

Scene-level properties (cdm_target_object, etc.) are registered
directly by operators.py to keep dependencies minimal.

This module is reserved for future per-object PropertyGroups if needed.
Currently it is a no-op stub that satisfies the import in __init__.py.
"""


def register():
    pass


def unregister():
    pass
